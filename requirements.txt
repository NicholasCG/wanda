# ============================================================
# Wanda – Python dependency file
# ============================================================
#
# STEP 1 – Create and activate a conda environment:
#   conda create -n prune_llm python=3.9 -y
#   conda activate prune_llm
#
# STEP 2 – Install PyTorch 1.10.1 with CUDA 11.3 via conda
#          (conda is preferred here to get the matching cudatoolkit):
#   conda install pytorch==1.10.1 torchvision==0.11.2 torchaudio==0.10.1 \
#       cudatoolkit=11.3 -c pytorch -c conda-forge -y
#
# STEP 3 – Install the remaining pip dependencies listed below:
#   pip install -r requirements.txt
# ============================================================

numpy<2
pyarrow<13
setuptools<81
transformers==4.28.0
datasets<3.0
wandb
sentencepiece
accelerate==0.18.0
lm_eval==0.4.2
peft==0.7.1
bitsandbytes==0.48.2
